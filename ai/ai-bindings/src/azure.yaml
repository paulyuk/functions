# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: aibindings-cs-ai-func
metadata:
  template: aibindings-cs-ai-func@0.0.1-beta
services:
  api:
    project: ./
    language: dotnet
    host: function
